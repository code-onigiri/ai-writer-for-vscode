# AI Writer provider credentials
# ------------------------------------------------------------
# 1. Copy this file to `.env` and fill in the required fields.
# 2. The VS Code extension loads secrets via SecretStorage; these
#    environment variables act as local fallbacks for development.
# 3. Never commit populated values to source control.

OPENAI_API_KEY=
GOOGLE_API_KEY=
GEMINI_CLI_PATH=
LMTAPI_ACCESS_TOKEN=
